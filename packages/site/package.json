{
  "name": "@pcamarajr/scout-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Marketing landing + docs reader for @pcamarajr/scout",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "typecheck": "astro check && tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "dependencies": {
    "@astrojs/mdx": "^6.0.0",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^6.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@biomejs/biome": "^2.0.0",
    "pagefind": "^1.5.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
